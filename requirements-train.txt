# Optional: only needed to retrain hab_xgb.json from NASA archive data.
# Install after requirements.txt:
#   pip install -r requirements-train.txt

-r requirements.txt

pandas>=2.0.0,<3
scikit-learn>=1.3.0,<2
